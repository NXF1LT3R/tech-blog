<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}}
    </div>
    <div class="text">
      {{text}}
    </div>
  </section>
  {{/each}}
</div>